<form method="post" action="{{ path('heberge_delete', {'id': heberge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ heberge.id) }}">
    <button class="btn">Delete</button>
</form>
